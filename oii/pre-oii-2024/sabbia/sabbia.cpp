#include <vector>

using namespace std;

long long alleggerisci(int n, int m, vector<int> v, vector<int> l, vector<int> r, vector<long long> k) {
    // n buildings
    // m zones
    // v weight
    // l r bounds (inclusive) , must be <= k

    // oh god they can overlap
    return 129;
}
